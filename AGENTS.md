# Agent Rules

## Git

- After every completed task: inspect `git status`, `git diff`, and
  `git log --oneline -10`; stage only intended files; commit; push.
- Use concise Conventional Commits. Never commit secrets or generated packages.
- Never force-push or skip hooks.
